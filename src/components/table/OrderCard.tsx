'use client';

import React from 'react';

// Componente vazio para resolver erro de importação
// Este componente foi removido e substituído pela implementação na página
export const OrderCard = () => {
  return null;
};

export default OrderCard;
