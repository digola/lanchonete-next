############################################
# .env para importação no Vercel (Produção)
# Copie este arquivo para .env.vercel e preencha
# os valores reais antes de importar no Vercel.
#
# Dica: rode `node scripts/generate-secrets.js`
# para gerar JWT_SECRET e NEXTAUTH_SECRET fortes.
############################################

# Ambiente
NODE_ENV="production"

# Database (PostgreSQL em produção)
# IMPORTANTE (Supabase):
# - Pooled (runtime): use o host do PgBouncer na porta 6543
#   Ex.: postgresql://postgres:<SENHA>@<POOLER_HOST>:6543/postgres?sslmode=require&pgbouncer=true&connection_limit=1&schema=public
# - Direto (migrations): use o host do banco na porta 5432
#   Ex.: postgresql://postgres:<SENHA>@db.<PROJECT-REF>.supabase.co:5432/postgres?sslmode=require&schema=public
# Substitua <POOLER_HOST>, <PROJECT-REF> e <SENHA> pelos valores reais do seu projeto.
DATABASE_URL="postgresql://postgres:<SENHA>@<POOLER_HOST>:6543/postgres?sslmode=require&pgbouncer=true&connection_limit=1&schema=public"
DIRECT_URL="postgresql://postgres:<SENHA>@db.<PROJECT-REF>.supabase.co:5432/postgres?sslmode=require&schema=public"

# JWT
JWT_SECRET="change-me"
JWT_EXPIRES_IN="7d"
JWT_REFRESH_EXPIRES_IN="30d"

# NextAuth
NEXTAUTH_SECRET="change-me"
# Substitua pelo domínio do seu projeto no Vercel
NEXTAUTH_URL="https://lanchonete-next-huf5ymo2u-sidnei-f-lopes-projects.vercel.app"

# Uploads
# Em Vercel, evite armazenamento local. Use provedores externos.
UPLOAD_MAX_SIZE="10485760" # 10MB
UPLOAD_ALLOWED_TYPES="image/jpeg,image/png,image/webp"

# App
# Se precisar usar o nome do app no cliente (browser), use NEXT_PUBLIC_APP_NAME
# para que fique disponível no bundle do front-end.
APP_NAME="Sabores Do Mundo"
APP_URL="https://lanchonete-next-huf5ymo2u-sidnei-f-lopes-projects.vercel.app"
# NEXT_PUBLIC_APP_NAME="Sabores Do Mundo"

# Email (opcional)
SMTP_HOST=""
SMTP_PORT=""
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM=""

# Pagamentos (opcional)
STRIPE_SECRET_KEY=""
STRIPE_PUBLISHABLE_KEY=""
PAGSEGURO_TOKEN=""

# Analytics (opcional)
GOOGLE_ANALYTICS_ID=""

# Observações:
# - Preencha todos os valores acima com credenciais reais.
# - NÃO comite .env.vercel com segredos; use este arquivo de exemplo.
# - No Vercel, importe estas variáveis em "Project Settings → Environment Variables".